---
const props = Astro.props
// if (!props.node || !props.node.asset || !props.node.asset._id) {
//   <pre>Oops, video</pre>
// }
---
<figure class="video-block">
  <video src={props.node.asset.url} controls preload="metadata"></video>
  {props.node.caption && <figcaption>{props.node.caption}</figcaption>}
</figure>